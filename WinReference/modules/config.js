// modules/config.js
// 职责：集中管理项目中所有可调整的参数，方便快速微调。

export const AppConfig = {

    // =======================================================================
    // 一、常规模式配置 (Normal Mode)
    // =======================================================================

    // --- 标签系统 ---
    LABEL_FONT_BASE_SIZE: 14,       // 说明: 标签的基础字体大小(px)。 默认值: 14
    LABEL_FONT_MAX_SIZE: 22,        // 说明: 摄像机拉近时标签的最大字体大小(px)。 默认值: 22
    MAX_LABELS_DESKTOP: 80,         // 说明: 桌面端同屏最多显示的标签数量。 (值越大, 显示越多)。 默认值: 80
    MAX_LABELS_MOBILE: 40,          // 说明: 移动端同屏最多显示的标签数量。 (值越大, 显示越多)。 默认值: 40
    LABEL_STICKY_DURATION_MS: 900,  // 说明: 标签在屏幕上“粘滞”显示的时长(毫秒)。 (值越大, 留存越久)。 默认值: 900
    FOCUSED_COUNTRY_CITY_SCORE_BOOST: 10.0, // 说明: 点击国家后，该国城市标签的显示优先级倍数。(值越大, 城市越易显示)。 默认值: 10.0
    LABEL_DEFAULT_DENSITY: 'default', // 说明: 页面加载时的默认密度 ('none', 'less', 'default', 'more')。 默认值: 'default'
    LABEL_DENSITY_MODIFIERS: {
        less: 0.4,    // “少”模式下的数量系数 (基础数量 * 0.6)
        default: 1.0, // “默认”模式下的数量系数
        more: 2     // “多”模式下的数量系数 (基础数量 * 1.5)
    },

    // --- 夜间模式 (常规模式下的切换) ---
    NIGHT_MODE_EMISSIVE_INTENSITY: 0.5, // 说明: 夜间模式下，地球城市灯火的亮度。 (值越大, 灯火越亮)。 默认值: 0.5
    NIGHT_MODE_AMBIENT_LIGHT: 0.01,   // 说明: 夜间模式下，环境光的亮度。 (值越大, 暗部越亮)。 默认值: 0.01

    // --- 云层效果 ---
    CLOUD_ROTATION_SPEED: 0.0002,   // 说明: 云层相对于地球的旋转速度。 (值越大, 转得越快)。 默认值: 0.0002

    // --- 相机控制 ---
    CONTROLS_MIN_DISTANCE: 3.0,     // 说明: 摄像机允许拉近的最小距离。 (值越小, 能拉得越近)。 默认值: 3.0
    CONTROLS_MAX_DISTANCE: 12.0,    // 注意: 此参数目前未生效，代码中硬编码为20.0。 默认值: 12.0


    // =======================================================================
    // 二、禅定模式专属配置 (Zen Mode)
    // =======================================================================

    ZEN_MODE_CONFIG: {
        auto_rotation_speed: 0.0008,    // 说明: 地球自动旋转的速度。 (值越大, 转得越快)。 默认值: 0.0008
        sun_light_intensity: 20.5,      // 说明: 主光源(太阳)的亮度。 (值越大, 白天越亮)。 默认值: 20.5
        sun_light_position_x: 10,       // 说明: 太阳光在X轴上的位置。 (值越大, 离地球越远)。 默认值: 10
        ambient_light_intensity: 0.01,  // 说明: 禅定模式下的基础环境光。 (值越大, 暗部越亮)。 默认值: 0.05
        night_lights_intensity: 0.4,    // 说明: 禅定模式下地球暗面城市灯火的亮度。 (值越大, 灯火越亮)。 默认值: 0.4

        // --- 辅助填充光 (用于增强立体感) ---
        zen_fill_light_intensity: 0.01,      // 说明: 辅助光的亮度。 (值越大, 补充光越强)。 默认值: 1.0
        zen_fill_light_color: '#ffffff',    // 说明: 辅助光的颜色。 默认值: '#ffffff'
        zen_fill_light_pos_x: -10,          // 说明: 辅助光的位置X (左/右)。 默认值: -10
        zen_fill_light_pos_y: 10,           // 说明: 辅助光的位置Y (上/下)。 默认值: 10
        zen_fill_light_pos_z: 5,            // 说明: 辅助光的位置Z (前/后)。 默认值: 5
        zen_fill_light_target_x: 0,         // 说明: 辅助光的目标点X。 默认值: 0
        zen_fill_light_target_y: 0,         // 说明: 辅助光的目标点Y。 默认值: 0
        zen_fill_light_target_z: 0,         // 说明: 辅助光的目标点Z。 默认值: 0
    },

    // --- 地球仪自身配置 (禅定模式) ---
    GLOBE_CONFIG: {
        axial_tilt_degrees: 23.5, // 说明: 地轴的倾斜角度(度)。 默认值: 23.5
        base_y_position: -3.5,    // 说明: 整个支架底座的垂直位置。 (值越小, 位置越低)。 默认值: -3.5
    },

    // --- 地球仪支架材质 (禅定模式) ---
    GLOBE_STAND_MATERIAL: {
        color: '#c5b358',       // 说明: 支架的金属颜色。 默认值: '#c5b358'
        metalness: 1.0,         // 说明: 金属感强度 (0-1)。 (值越大, 金属感越强)。 默认值: 1.0
        roughness: 0.4          // 说明: 表面粗糙度 (0-1)。 (值越小, 表面越光滑、反光越强)。 默认值: 0.4
    },

    // --- 地球仪支架几何尺寸 (禅定模式) ---
    GLOBE_STAND_GEOMETRY: {
        base_part1_radius: 1.8,     // 说明: 底座最下层圆盘的半径。 (值越大, 圆盘越宽)。 默认值: 1.8
        base_part1_height: 0.06,    // 说明: 底座最下层圆盘的高度。 (值越大, 圆盘越厚)。 默认值: 0.06
        base_part2_radius: 1.7,     // 说明: 底座上层圆盘的半径。 (值越大, 圆盘越宽)。 默认值: 1.7
        base_part2_height: 0.1,     // 说明: 底座上层圆盘的高度。 (值越大, 圆盘越厚)。 默认值: 0.1
        cloud_offset: 0.05,         // 说明: 地球表面与云层之间的间隙。 (值越大, 间隙越大)。 默认值: 0.05
        ring_gap: 0.15,             // 说明: 云层与子午圈之间的间隙。 (值越大, 间隙越大)。 默认值: 0.15
        ring_width: 0.14,           // 说明: 子午圈金属环本身的宽度。 (值越大, 金属环越粗)。 默认值: 0.14
        pivot_height: 0.3,          // 说明: 南北极连接处的转轴圆柱的高度。 (值越大, 转轴越长)。 默认值: 0.3
        pivot_radius: 0.07,         // 说明: 南北极转轴的半径。 (值越大, 转轴越粗)。 默认值: 0.07
        support_radius: 0.1,        // 说明: 连接底座和子午圈的垂直支撑杆的半径。 (值越大, 支撑杆越粗)。 默认值: 0.1
    },

    // --- 诗句动画专属配置 ---
    POETRY_CONFIG: {
        lifecycle_duration: 20000,    // 说明: 每句诗从出现到消失的总时长(毫秒)。(值越大, 移动越慢)。默认值: 20000
        fade_duration: 3000,        // 说明: 每句诗淡入和淡出的动画时长(毫秒)。(值越大, 过渡越舒缓)。默认值: 3000
        spawn_delay: 1500,          // 说明: 一句诗消失后，到下一句诗出现前的间隔(毫秒)。默认值: 1500
        // 说明: 诗句浮现的安全区域, 0=最左/上, 1=最右/下。
        spawn_area: {
            x: [0.2, 0.8], // 水平方向上，只在屏幕宽度的20%到80%之间出现
            y: [0.2, 0.8]  // 垂直方向上，只在屏幕高度的20%到80%之间出现
        }
    },


    // =======================================================================
    // 三、全局通用设置 (Global)
    // =======================================================================
    OPTIMIZATION: {
        INTERACTION_THROTTLE_MS: 50, // 说明: 鼠标移动事件的节流间隔(毫秒)。(值越大, 性能越好但响应可能稍有延迟)。 默认值: 50
    }
};